# pytest.ini
[pytest]
addopts = -v --ignore=.venv/
# add -rP to addopts if you need the output from console
filterwarnings = ignore::pytest.PytestCollectionWarning
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S